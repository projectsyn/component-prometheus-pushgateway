apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: prometheus-pushgateway
    chart: prometheus-pushgateway-1.5.1
    heritage: Tiller
    release: platform
  name: platform-prometheus-pushgateway
